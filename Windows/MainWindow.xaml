<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WocForC_.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WocForC_.Windows"
    xmlns:views="using:WocForC_.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WocForC_">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base"/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TitleBar
            x:Name="FilesManageTitleBar"
            Title="FilesManage"
            Background="Azure"
            Subtitle=""
            IsBackButtonVisible="True"
            IsBackButtonEnabled="{x:Bind rootFrame.CanGoBack, Mode=OneWay}"
            BackRequested="AppTitleBar_BackRequested">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/Tiles/GalleryIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>
        <Frame x:Name="rootFrame" Grid.Row="1"
               SourcePageType="views:FilesManager"
               />
    </Grid>
</Window>
